package logic.restrictions;

import model.entities.player.Player;

/**
 * Interface that all {@link Player} restrictions must implement.
 * {@link Player} restrictions interface.
 * Extends {@link Restrictions}.
 */
public interface PlayerRestrictions extends Restrictions<Player> {
}
