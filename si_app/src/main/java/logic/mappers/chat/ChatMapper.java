package logic.mappers.chat;

import logic.mappers.DataMapper;
import model.entities.chat.Chat;

/**
 * {@link Chat} mapper interface to be used by the data mappers.
 */
public interface ChatMapper extends DataMapper<Chat, Integer> {
}
